name: CI-CD

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop, main ]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-analyze-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout con historial completo
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Instalo JDK 21 (Temurin) y activo cache de Gradle
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      # Permisos al wrapper
      - name: Gradle permissions
        run: chmod +x gradlew

      # --- CI: tests + cobertura ---
      - name: Unit tests + coverage (JaCoCo)
        run: ./gradlew clean test jacocoTestReport --no-daemon

      # Subo reportes como artefactos
      - name: Upload test & coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            build/test-results/test
            build/reports/tests/test
            build/reports/jacoco/test

      # Análisis en SonarCloud
      - name: SonarCloud
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=juansrg95_ReservaCanchaF
            -Dsonar.organization=juansrg95
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.gradle.skipCompile=true
            -Dsonar.java.binaries=build/classes/java/main
            -Dsonar.junit.reportPaths=build/test-results/test
            -Dsonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/test/jacocoTestReport.xml

      # --- CD: solo en develop ---
      - name: Build JAR (release)
        if: github.ref == 'refs/heads/develop'
        run: ./gradlew clean build -x test --no-daemon

      # Tomo la ruta del JAR generado (no el -plain)
      - name: Pick built JAR
        if: github.ref == 'refs/heads/develop'
        id: jar
        run: |
          echo "jarfile=$(ls -1 build/libs/*.jar | grep -v '\-plain\.jar' | head -n 1)" >> $GITHUB_OUTPUT
          echo "Picked ${{ steps.jar.outputs.jarfile }}"

      # Copio el JAR a la EC2 (appleboy espera que target sea directorio)
      - name: Upload JAR to EC2
        if: github.ref == 'refs/heads/develop'
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: ${{ steps.jar.outputs.jarfile }}
          target: /opt/reservacancha

      # Renombro a app.jar y reinicio el servicio
      - name: Restart service on EC2
        if: github.ref == 'refs/heads/develop'
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            LATEST_JAR=$(ls -1t /opt/reservacancha/*.jar | head -n 1)
            sudo mv "$LATEST_JAR" /opt/reservacancha/app.jar
            sudo systemctl daemon-reload
            sudo systemctl restart reservacancha
            sudo systemctl status reservacancha --no-pager || true

      # Smoke tests post-deploy
      - name: "Smoke test: /health"
        if: github.ref == 'refs/heads/develop'
        run: curl -sS -I http://${{ secrets.EC2_HOST }}:8080/health | head -n 1

      - name: "Smoke test: OpenAPI"
        if: github.ref == 'refs/heads/develop'
        run: curl -sS -I http://${{ secrets.EC2_HOST }}:8080/v3/api-docs | head -n 1







